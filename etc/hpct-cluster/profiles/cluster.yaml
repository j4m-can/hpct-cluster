description: Basic LXD-based cluster
config_dir: cluster
work_dir: work/cluster

interview:
  path: cluster/interview/interview.yaml

charms:
  home: work/cluster/charms
  builder_path: cluster/charms-builder.yaml
  bundle_name: bundle.yaml

juju:
  cloud: localhost
  #controller: cluster
  controller: default
  #model: default
  model: cluster
  user: clusteradmin

lxd:
  user: jdm
