description: Basic LXD-based cluster

juju:
  cloud: localhost
  controller: default
  model: cluster
  user: clusteradmin
  #bootstrap-series: jammy
  #bootstrap-series: oracle-8

lxd:
  user: jdm

charms:
  run-on: ubuntu-22.04-amd64_oracle-8-amd64
