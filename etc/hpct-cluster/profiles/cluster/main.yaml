description: Basic LXD-based cluster

juju:
  cloud: localhost
  #controller: cluster
  controller: default
  #model: default
  model: cluster
  user: clusteradmin

lxd:
  user: jdm

charms:
  run-on: ubuntu-22.04-amd64_oracle-8-amd64
